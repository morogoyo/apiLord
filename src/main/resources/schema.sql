CREATE TABLE IF NOT EXISTS api_lord.measurements (
                         id int,
                         comments varchar(255),
                         subject varchar(255),
                         length double,
                         area double,
                         count double
);